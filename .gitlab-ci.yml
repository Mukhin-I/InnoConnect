stages:
  - build
  - test
  - deploy

variables:
  DOCKER_DRIVER: overlay2
  COMPOSE_DOCKER_CLI_BUILD: 1
  DOCKER_BUILDKIT: 1

# Reusable Docker-in-Docker setup
.docker_setup: &docker_setup
  image: docker:27
  services:
    - docker:27-dind

build_backend:
  <<: *docker_setup
  stage: build
  script:
    - ls
    - cp .env.example .env
    - docker compose --env-file .env -f backend/deployment/docker/docker-compose.yml build
  # optional: catch build failures early without pushing anywhere
list_secure_files:
  stage: build
  image: alpine:latest
  script:
    - apk add --no-cache curl jq
    - >
      curl --header "JOB-TOKEN: $CI_JOB_TOKEN"
      "https://$CI_SERVER_FQDN/api/v4/projects/$CI_PROJECT_ID/secure_files" | jq .
build_frontend:
  <<: *docker_setup
  image: registry.gitlab.com/gitlab-org/cli:latest
  stage: build
  
  script:
    
    - glab auth login --job-token $CI_JOB_TOKEN --hostname $CI_SERVER_FQDN --api-protocol $CI_SERVER_PROTOCOL
    - glab -R $CI_PROJECT_PATH securefile download $.env --path="frontend/.env"
    - chmod 600 /cd
    - ls
    - cd frontend && npm run build
  # optional: catch build failures early without pushing anywhere

test:
  <<: *docker_setup
  stage: test
  cache:
    key: docker-layer-cache
    paths:
      - .docker-cache/
  script:
    - ls
    - docker compose --env-file .env -f backend/deployment/docker/docker-compose.yml build --build-arg BUILDKIT_INLINE_CACHE=1
    - echo "Testing"
  
deploy:
  stage: deploy
  image: registry.gitlab.com/gitlab-org/cli:latest
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_COMMIT_BRANCH == "feat/36-CI/CD"'
  before_script:
    - glab auth login --job-token $CI_JOB_TOKEN --hostname $CI_SERVER_FQDN --api-protocol $CI_SERVER_PROTOCOL
    - glab -R $CI_PROJECT_PATH securefile download $SECURE_FILE_ID --path="/cd"
    - chmod 600 /cd
    - eval $(ssh-agent -s)
    - ssh-add /cd
    - mkdir -p ~/.ssh
    - ssh-keyscan -H "$DEPLOY_HOST" >> ~/.ssh/known_hosts
  script:
    - ssh "$DEPLOY_USER@$DEPLOY_HOST" "cd /innoconnect && echo ALL OK"#&& git pull && docker compose up -d --build && docker compose up -d --build"